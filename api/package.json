{
  "name": "attribution-middleware-api",
  "version": "1.0.0",
  "description": "Marketing Attribution Middleware demo: GHL webhooks + first-touch capture + dashboard API",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "test": "node --env-file-if-exists=.env --test"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "redis": "^4.7.0"
  }
}
